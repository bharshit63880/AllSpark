###################################################
# Services
###################################################
services:
  ###################################################
  # Service: zookeeper
  ###################################################
  zookeeper:
    image: zookeeper
    ports:
      - "2181:2181"
    restart: unless-stopped

  ###################################################
  # Service: kafka
  ###################################################
  kafka:
    image: confluentinc/cp-kafka:7.4.0
    depends_on:
      zookeeper:
        condition: service_started
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: "zookeeper:2181"

      KAFKA_LISTENERS: "PLAINTEXT://0.0.0.0:9092"
      KAFKA_ADVERTISED_LISTENERS: "PLAINTEXT://kafka:9092"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "PLAINTEXT:PLAINTEXT"
      KAFKA_INTER_BROKER_LISTENER_NAME: "PLAINTEXT"

      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_MIN_INSYNC_REPLICAS: 1
      KAFKA_DEFAULT_REPLICATION_FACTOR: 1
      KAFKA_NUM_PARTITIONS: 3
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_LOG_RETENTION_HOURS: 24

      KAFKA_NUM_NETWORK_THREADS: 8
      KAFKA_NUM_IO_THREADS: 8
      KAFKA_SOCKET_SEND_BUFFER_BYTES: 102400
      KAFKA_SOCKET_RECEIVE_BUFFER_BYTES: 102400
      KAFKA_SOCKET_REQUEST_MAX_BYTES: 104857600

    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server localhost:9092 --list >/dev/null 2>&1"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 60s

    restart: unless-stopped
    networks:
      - default

  ###################################################
  # Service: redis
  ###################################################
  redis:
    image: redis/redis-stack
    ports:
      - "6379:6379"
      - "8001:8001"
    restart: unless-stopped

  ###################################################
  # Service: db
  ###################################################
  db:
    image: mongo
    ports:
      - "27017:27017"
    restart: unless-stopped

  ###################################################
  # Service: mailhog (local SMTP for dev/testing)
  ###################################################
  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025"
      - "8025:8025"
    restart: unless-stopped

  ###################################################
  # Service: ui
  ###################################################
  ui:
    build:
      context: ./app/ui
      target: ui-dev
    ports:
      - 5173:5173
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: api
  ###################################################
  api:
    build:
      context: ./app/api
      target: api-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 8000:8000
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: auth
  ###################################################
  auth:
    build:
      context: ./services/auth
      target: auth-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3100:3100
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: permissions
  ###################################################
  permissions:
    build:
      context: ./services/permissions
      target: permissions-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3400:3400
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: email
  ###################################################
  email:
    build:
      context: ./services/email
      target: email-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - 3600:3600
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: users
  ###################################################
  users:
    build:
      context: ./services/users
      target: users-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3900:3900
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: problems
  ###################################################
  problems:
    build:
      context: ./services/problems
      target: problems-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3500:3500
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: contests
  ###################################################
  contests:
    build:
      context: ./services/contests
      target: contests-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3200:3200
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: leaderboard
  ###################################################
  leaderboard:
    build:
      context: ./services/leaderboard
      target: leaderboard-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3800:3800
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: submissions
  ###################################################
  submissions:
    build:
      context: ./services/submissions
      target: submissions-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3700:3700
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: support-ticket
  ###################################################
  support-ticket:
    build:
      context: ./services/support-ticket
      target: support-ticket-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3960:3960
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: special-access
  ###################################################
  special-access:
    build:
      context: ./services/special-access
      target: special-access-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3970:3970
    env_file:
      - ./main.conf
    restart: unless-stopped

  ###################################################
  # Service: judge
  ###################################################
  judge:
    build:
      context: ./services/judge
      target: judge-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3300:3300
    env_file:
      - ./main.conf
    restart: unless-stopped


      ###################################################
  # Service: admin-service
  ###################################################
  admin-service:
    build:
      context: ./services/admin-service
      target: admin-dev
    depends_on:
      kafka:
        condition: service_healthy
      redis:
        condition: service_started
      db:
        condition: service_started
    ports:
      - 3950:3950
    env_file:
      - ./main.conf
    restart: unless-stopped
